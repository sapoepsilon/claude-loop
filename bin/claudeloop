#!/usr/bin/env bash
set -euo pipefail
SCRIPT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
REPO_DIR="$( cd "${SCRIPT_DIR}/.." && pwd )"
exec node --import "${REPO_DIR}/node_modules/tsx/dist/loader.mjs" "${SCRIPT_DIR}/claudeloop.ts" "$@"
